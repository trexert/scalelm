apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-service"
spec:
  type: "NodePort"
  selector:
    app: "llm"
  ports:
    - port: 11434
      targetPort: 11434
      nodePort: 30434
